import React, { useEffect, useRef, useState } from "react";

const Alarms = () => {
  return (
    <div className="container noti">
      <div className="title noti">Alarms</div>
      <form className="list noti"></form>
    </div>
  );
};

export default Alarms;
